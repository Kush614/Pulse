# API Layer

Owner: Person 2

Responsibilities:

- Call PULSE backend endpoints
- Wrap external services used by the frontend integration layer
- Normalize source-bias lookup, GDELT tone/context responses, and Apify article extraction responses

Keep all network code here so screen code stays clean.
